import { Loader } from '@fluentui/react-northstar';
import * as React from 'react';

const LoaderExampleInline: React.FC<React.PropsWithChildren<unknown>> = () => <Loader inline />;

export default LoaderExampleInline;
